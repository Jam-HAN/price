/**
 * 거래처별 단가표 파싱 프롬프트. 각 거래처마다 포맷이 달라서 통신사 단위로 분리.
 * 공통: 모든 금액은 원(KRW) 단위로 변환해서 출력.
 */

const COMMON_INSTRUCTIONS = `
이 이미지는 대한민국 휴대폰 대리점에서 배포하는 **단가표**(정책표)입니다.
구조화된 JSON으로 정보를 추출해 주세요.

단위 규칙(매우 중요):
- **출고가** 컬럼(예: "1,254", "1,254,000"): 이미지에 "천단위"/"천원단위" 표기 있으면 ×1,000, 이미 원 단위면 그대로
- **요금지원금/공시지원금**(예: 500, 490): 보통 천원 단위. ×1,000 해서 저장. 예: 500 → 500,000원
- **단가(공통/선약 하의 010·MNP·기변 숫자)**: 만원 단위. ×10,000 해서 저장. 예: 5 → 50,000원. 음수는 페이백(예: -10 → -100,000원)
- 모든 결과 금액은 **원(KRW) 정수**로

개통유형 정규화:
- "010" / "010신규" / "신규" → new010
- "MNP" → mnp
- "기변" / "보상" / "재가입" → change

약정 유형:
- "공통" / "공시" / "공시지원금" → common
- "선약" / "선택약정" → select
- 거래처가 선약을 명시 안 하면 common만 채우고 select는 null

요금제 구간 코드 정규화: 스키마의 description을 따를 것. 원본 라벨은 plan_tier_raw에 보존.

숫자를 못 읽으면 해당 필드를 null로 두되, 모델 행 자체는 생략하지 말 것.
`;

export const PROMPTS: Record<string, string> = {
  SKT: `${COMMON_INSTRUCTIONS}

이 단가표는 **SKT(통신사)** 거래처입니다. 특징:
- 요금제 구간 최대 7개: BASE(요금제 불입정책 포함단가) / I_100(프라이밍 플러스 청년99) / F_79(프라이밍 청년89) / L_69(레귤러플러스) / M_50(베이직플러스) / R_43(컴팩트/베이직) / S_33(세이브/ZEM플랜스마트)
- 각 구간별로 "요금지원금" 컬럼(천원 단위) + "공통(010/MNP/기변)" 3칸 + "선약(010/MNP/기변)" 3칸 = 7개 숫자
- 부가정책: 마이스마트콜3 + T All Care 플러스 6 세트 유치(보통 4만), 추가지원 플러스 정책(특정 모델군 +5만), 워치번들, 유무선 동시판매, OSS 등

모델 코드는 이미지 좌측 "구분"/"모델명" 컬럼(예: SM-S942N, SM-S942N_512G, IP17_256GB) 그대로 model_code_raw에 보존.
"팻네임"이 있으면 nickname에 (예: "갤럭시 S26", "아이폰17 256G").`,

  KT: `${COMMON_INSTRUCTIONS}

이 단가표는 **KT(통신사)** 거래처입니다. 특징:
- 요금제 구간은 **금액대** 기준으로 4~5개: T110(110K/초이스 스페셜) / T100(100K/스페셜) / SLIM14(5G 슬림 14G) / T61(61K/심플30GB) / T37(37K/슬림·시니어)
- 약정 표기는 거래처마다 다름. 공통/할원 2행 나뉜 경우(반추) "공통"은 공시지원금, "할원"은 할부원금 참고값 — 할원은 무시하고 공통지원금만 subsidy_krw로 저장
- 단가는 한 셀에 큰 숫자(010신규/MNP/보상) 형태로 표기
- 부가정책: 제휴카드(SAVE 할부카드/DC 후청구), 추가지원금 활성화, 유튜브 프리미엄, OTT 구독, 부가서비스(필수팩/365 폰케어)

개통유형: 010신규 → new010, MNP → mnp, 보상/기변 → change.`,

  'LGU+': `${COMMON_INSTRUCTIONS}

이 단가표는 **LG U+(통신사)** 거래처입니다. 특징:
- 요금제 구간은 **군** 단위 7개: G115(최고가군/시그니처) / G105(프리미어 플러스) / G95(프리미어 레귤러) / G85(프리미어 에센셜/75·85군) / G69(스탠다드/61·69군) / G55(라이트+/44·55군) / G33(키즈/청소년/33군)
- 각 군에 "공시" + "단가(신규/MNP/재가입)"
- **LGU+는 "재가입"이 곧 기변**. change로 매핑할 것
- 선약 별도 컬럼이 없으면 common만
- 부가정책: IB캠페인, 청소년·시니어 요금제 추가/축소, 유통망지원금, 선택약정 축소, 버디/A175 축소, 이통사지원금 추가, 번들 정책, 중고 MNP 결합

거래처별 특이:
- **대산**: 그룹 1~7 표기. 청소년군도 명시.
- **안성**: "최고가군/고가군/중저가군/저가군/최저가군/33군" 표기. 하단에 축소 정책 상세.

model_code_raw: 이미지의 원본 코드(SM-S942N256, UIP17E 등).`,
};
