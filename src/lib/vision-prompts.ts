/**
 * 거래처별 단가표 파싱 프롬프트. 각 거래처마다 포맷이 달라서 통신사 단위로 분리.
 * 공통: 모든 금액은 원(KRW) 단위로 변환해서 출력.
 */

const COMMON_INSTRUCTIONS = `
이 이미지는 대한민국 휴대폰 대리점에서 배포하는 **단가표**(정책표)입니다.
구조화된 JSON으로 정보를 추출해 주세요.

==============================
단위 규칙 (매우 중요 · 반드시 지킬 것)
==============================
모든 결과 금액은 **원(KRW) 정수**로 저장. 이미지의 숫자는 컬럼 유형에 따라 단위를 곱해서 원으로 변환.

1) **출고가** (모델 옆 컬럼, 보통 1,000 ~ 2,500 범위):
   - 이미지 헤더에 "천단위" / "(천원)" 표기 → ×1,000
   - 이미지 값이 이미 6~7자리(예: 1,254,000) → 그대로
   - 예상 범위: 300,000 ~ 3,200,000원
   - ⚠️ **반드시 이미지에서 직접 읽은 값만 사용할 것**. 기억·상식·공식가 추측으로 채우지 말 것.
     이미지에 출고가 컬럼이 없거나 값을 읽을 수 없으면 **null**로 반환 (0이 아님).

2) **요금지원금 / 공시지원금** (각 요금제 구간에 하나씩 있는 값):
   - 이미지에 "500", "490", "450", "250" 같은 작은 숫자로 표기됨 — **이것은 천원 단위**
   - 반드시 ×1,000 해서 원으로 변환
   - 예상 범위: **50,000 ~ 600,000원** (보통 300k~500k)
   - ⚠️ 추출한 값이 5,000원·60,000원처럼 10만 미만으로 나오면 ×10을 덜 곱한 것. 다시 확인.
   - 예: "500" → 500,000 (50,000 아님)
   - 예: "490" → 490,000
   - 예: "220" → 220,000

3) **단가 (공통/공시 × 010·MNP·기변, 선약 × 010·MNP·기변)**:
   - 이미지에 "5", "60", "41", "-10" 같은 -50 ~ +150 범위의 작은 정수
   - **이것은 만원 단위** → ×10,000
   - 예: "5" → 50,000 (5,000 아님)
   - 예: "60" → 600,000
   - 예: "-10" → -100,000 (페이백)

4) **정책 금액** (부가정책 섹션의 "+4만", "+15만", "30만" 같은 표기):
   - "만" 명시 있으면 ×10,000
   - 예: "4만" → 40,000
   - 예: "15만" → 150,000

==============================
개통유형 정규화
==============================
- "010" / "010신규" / "신규" → new010
- "MNP" / "번이" → mnp
- "기변" / "보상" / "재가입" (LGU+는 "재가입"이 곧 기변) → change

==============================
약정 유형
==============================
- "공통" / "공시" / "공시지원금" / "공통지원금" → common
- "선약" / "선택약정" → select
- 거래처가 선약 컬럼을 제공하지 않으면 common만 채우고 select는 null

==============================
요금제 구간 코드
==============================
스키마 description의 enum에서 고를 것. 원본 이미지 라벨은 버려도 됨 (이제 스키마에 plan_tier_raw 없음).

==============================
기타
==============================
- 숫자를 못 읽으면 해당 필드만 null. 모델 행 자체는 생략하지 말 것.
- 워치/태블릿 번들 섹션이 있으면 그것도 models에 포함 (storage에 "워치" 표시)
- 시니어/청소년 전용 모델도 포함
`;

/**
 * ⚠ 현재 모드: **최상위 구간만 파싱** (정확도 우선)
 * SKT: BASE / KT: T110 / LGU+: G115
 * 나머지 구간 무시. Claude 주의력 집중 → 단가 misread 감소.
 */

const TOP_TIER_RULE = `
==============================
★★★ 최상위 구간만 파싱 ★★★
==============================
**반드시 각 모델마다 "최상위 요금제 구간 1개"만 tiers 배열에 담을 것**.
중·하위 구간(예: I_100, F_79, L_69, M_50, R_43, S_33)은 **완전히 무시**.
결과 tiers 배열은 **모델당 정확히 1개 원소**여야 함.

이 제약으로 인한 이점:
- Claude가 컬럼 42셀을 한번에 추적할 필요 없이 6셀만 집중 → 정확도 크게 향상
- 선두자리 misread / 공통↔선약 혼동 감소

만약 최상위 구간 행이 이미지에서 확실히 보이지 않으면 해당 tier 의 값을 null 로.
`;

export const PROMPTS: Record<string, string> = {
  SKT: `${COMMON_INSTRUCTIONS}
${TOP_TIER_RULE}

이 단가표는 **SKT(통신사)** 거래처입니다 (피에스·청담 등).

==============================
이번에 파싱할 구간 (SKT 최상위 1개)
==============================
**plan_tier_code = "BASE"** — "신규요금제불입정책 포함단가"
(5GX 프리미엄 청년109, 0청년109 요금제 구간)

이 구간의 열은 좌측부터 7개 숫자:
① 요금지원금 (천단위) → ×1,000
② 공통 010 (만원) → ×10,000
③ 공통 MNP (만원) → ×10,000
④ 공통 기변 (만원) → ×10,000
⑤ 선약 010 (만원) → ×10,000
⑥ 선약 MNP (만원) → ×10,000
⑦ 선약 기변 (만원) → ×10,000

이미지에서 BASE 행 다음에 오는 I_100, F_79, L_69, M_50, R_43, S_33 구간은 **전부 무시**.

==============================
레퍼런스 예시 (사용자 확인된 정답)
==============================
SM-S942N (갤럭시 S26, 출고가 1,254) 행의 BASE 구간 7개 숫자는:
**[500, 5, 60, 41, 40, 48, 48]**

따라서 JSON은 (tiers 배열에 BASE 1개만):
{
  "model_code_raw": "SM-S942N",
  "nickname": "갤럭시 S26",
  "retail_price_krw": 1254000,
  "tiers": [
    {
      "plan_tier_code": "BASE",
      "subsidy_krw": 500000,
      "common": { "new010": 50000, "mnp": 600000, "change": 410000 },
      "select": { "new010": 400000, "mnp": 480000, "change": 480000 }
    }
  ]
}

==============================
추출 규칙
==============================
- 셀이 비어있거나 "−"/"·"이면 해당 개통유형 값 null
- 음수 숫자(-5, -10)는 페이백. ×10,000 적용 (-50,000)
- 부가정책은 policies 배열에

모델 코드 원본 그대로: SM-S942N, SM-S947N, SM-S948N_512G, IP17_256GB, IP17P_512G 등`,

  KT: `${COMMON_INSTRUCTIONS}
${TOP_TIER_RULE}

이 단가표는 **KT(통신사)** 거래처입니다 (니어·반추 등).

==============================
이번에 파싱할 구간 (KT 최상위 1개)
==============================
**plan_tier_code = "T110"** — "초이스 스페셜" / "프리미엄" (110,000원 이상)

이 구간 안의 컬럼 순서 (좌→우):
- 공시지원금 또는 공통지원금 (천단위) → ×1,000
- 단가 [010신규, MNP, 보상/기변] (만원 단위) → ×10,000

이미지의 T100, SLIM14, T61, T37 등 나머지 구간은 **전부 무시**.

거래처 변형 주의:
- **반추**: 공통/할원 2행 표기 — "공통"이 공시지원금, "할원"은 무시. 단가는 큰 글씨로.
- **니어**: 공시지원금 + 단가가 같은 그룹 안에 정렬됨
- 약정 컬럼 별도 없으면 common만, select는 null

개통유형: 010신규 → new010, MNP → mnp, 보상/기변 → change.

부가정책(제휴카드, 추가지원금, OTT, 필수팩/365 폰케어)은 policies 배열에.`,

  'LGU+': `${COMMON_INSTRUCTIONS}
${TOP_TIER_RULE}

이 단가표는 **LG U+(통신사)** 거래처입니다 (대산·안성 등).

==============================
이번에 파싱할 구간 (LGU+ 최상위 1개)
==============================
**plan_tier_code = "G115"** — "시그니처 / 프리미어 슈퍼" (최고가군 115군 이상)

이 구간 안의 컬럼 순서 (좌→우):
- 공시 또는 공시지원금 (천단위) → ×1,000
- 단가 [신규(=010신규), MNP, 재가입(=기변)] (만원 단위) → ×10,000

이미지의 G105, G95, G85, G69, G55, G33 등 나머지 구간은 **전부 무시**.

**중요: LGU+의 "재가입"은 곧 기변**. activation_type='change'로 매핑.
선약 별도 컬럼 없으면 common만 채우고 select는 null.

거래처별 특이:
- **대산**: "그룹 1~7" 표기. 그룹 1이 G115.
- **안성**: "최고가군" 표기. 그게 G115.

부가정책은 policies 배열에 (IB캠페인, 청소년/시니어 추가 등).

model_code_raw: 이미지의 원본 코드 그대로 (SM-S942N256, UIP17E 등).`,
};
